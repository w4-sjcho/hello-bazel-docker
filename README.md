# Building a Linux docker image from Mac OS with Bazel

This repo demonstrates how to build a Linux docker image using Bazel from
Mac OS.  To use it, clone the repo and run:
```
./run_docker.sh
docker run $IMAGE
```
